#pragma once

// The parser is a pure, host-independent krkrz leaf.  Keep its one source of
// truth in the pinned submodule and expose it through this Aether include so
// callers never include the standalone upstream tree directly.
#include "../../../third_party/krkrz_dev/src/core/common/visual/FontVariations.h"

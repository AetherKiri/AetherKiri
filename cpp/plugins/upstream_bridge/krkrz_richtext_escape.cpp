// EscapeConverter has no host ABI of its own; compile the implementation
// directly from the pinned submodule so the parent repository does not carry a
// second copy.
#include <krkr_richtext/src/EscapeConverter.cpp>

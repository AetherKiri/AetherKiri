name: Linux Build

on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Linux Godot export not wired
    runs-on: ubuntu-24.04

    steps:
      - name: Report current status
        run: |
          echo "Linux Godot export is not wired in build/build_linux.sh yet."
          echo "This workflow is intentionally a no-op until the Linux export path is implemented."
